# frozen_string_literal: true

source "https://rubygems.org"

# Jekyll
gem "jekyll", "~> 4.3.2"

# Theme
gem "minima", "~> 2.5"

# GitHub Pages deployment (optional, only if deploying via GitHub Pages)
# gem "github-pages", group: :jekyll_plugins

# Development dependencies (optional but useful)
group :development do
  gem "webrick", "~> 1.7"  # Required for local Jekyll 4+ on Ruby >=3.0
end
